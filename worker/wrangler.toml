name = "aule-libere-polimi"
main = "src/index.ts"
compatibility_date = "2026-08-27"

# The Free plan applies its CPU ceiling automatically. Network wait time is
# not counted, but parsing and formatting are.

[observability.logs]
enabled = true
invocation_logs = true
